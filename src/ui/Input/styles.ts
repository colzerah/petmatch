import { StyleSheet } from 'react-native';

export const container = StyleSheet.create({
  objStyle: {},
}).objStyle;

export const content = StyleSheet.create({
  objStyle: {},
}).objStyle;

export const inputKitStyle = StyleSheet.create({
  objStyle: {
    borderRadius: 10,
  },
}).objStyle;
