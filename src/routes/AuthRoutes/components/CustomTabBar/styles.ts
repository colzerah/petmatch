import { StyleSheet } from 'react-native';

export const container = StyleSheet.create({
  objStyle: { flexDirection: 'row', height: 70, backgroundColor: '#703500' },
}).objStyle;

export const buttonTab = StyleSheet.create({
  objStyle: { flex: 1, alignItems: 'center', justifyContent: 'center' },
}).objStyle;

export const titleTab = StyleSheet.create({
  objStyle: {},
}).objStyle;
